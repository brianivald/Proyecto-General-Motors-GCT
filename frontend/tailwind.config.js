/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        gm: {
          blue: '#165D98', // Color corporativo primario
          light: '#6BA5D4', // Logo secundario
          bg: '#F4F7FB', // Fondo minimalista
          card: '#FFFFFF',
          dark: '#1A202C'
        }
      },
      animation: {
        'fade-in': 'fadeIn 0.4s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0', transform: 'translateY(5px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        }
      }
    },
  },
  plugins: [],
}
